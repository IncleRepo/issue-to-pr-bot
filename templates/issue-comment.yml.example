name: issue-comment-bot

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  run-bot:
    if: >
      vars.BOT_MENTION != '' &&
      contains(github.event.comment.body, vars.BOT_MENTION) &&
      (
        github.event.comment.author_association == 'OWNER' ||
        github.event.comment.author_association == 'MEMBER' ||
        github.event.comment.author_association == 'COLLABORATOR'
      )
    uses: IncleRepo/issue-to-pr-bot/.github/workflows/reusable-bot.yml@main
    secrets: inherit
    with:
      runner_labels_json: '["self-hosted","Windows"]'
      engine_repository: "IncleRepo/issue-to-pr-bot"
      engine_ref: "main"
